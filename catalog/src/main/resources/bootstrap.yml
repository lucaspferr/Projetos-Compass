spring:
  application:
    name: 'catalog'
  profiles:
    active: default
  cloud:
    config:
      uri: http://localhost:8888
  rabbitmq:
    host: localhost
    port: 5672
    username: guest
    password: guest