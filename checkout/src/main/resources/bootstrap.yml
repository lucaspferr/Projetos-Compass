spring:
  application:
    name: 'checkout'
  cloud:
    config:
      uri: http://localhost:8888
  rabbitmq:
    host: localhost
    port: 5672
    username: guest
    password: guest
---
spring:
  profiles: dev
  cloud:
    config:
      uri: http://localhost:8888